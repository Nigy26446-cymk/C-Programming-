/*
NAME : GABRIEL KARIUKI
REG NO. : CT101/G/26446/25
DESCRIPTION : A C Program that uses for loop to print numbers from 100 down to 50 in a descending order
*/

#include <stdio.h>

int main() {
    
	for (int i = 100; i >= 50; i--) {
        printf("%d\n", i);
    }
    return 0;
}